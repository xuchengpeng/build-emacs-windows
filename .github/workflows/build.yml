name: build

on:
  push:
    branches:
      - "**"
    paths-ignore:
      - "**/*.md"
      - "**/*.org"
  schedule:
    - cron: "0 0 * * *"

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: get emacs version
        run: |
          version=`curl -s https://cgit.git.savannah.gnu.org/cgit/emacs.git/plain/configure.ac | sed -n 's/^AC_INIT(\[[^]]*\], \[\([^]]*\)\], .*/\1/p'`
          commit=`git ls-remote https://git.savannah.gnu.org/git/emacs.git HEAD | awk '{print substr($1,1,7)}'`
          ts=`date +'%Y%m%d'`
          echo "emacs_ver=${version}-${ts}-${commit}" >> $GITHUB_ENV
    outputs:
      emacs_ver: ${{ env.emacs_ver }}
  build:
    runs-on: windows-latest
    needs: prepare
    strategy:
      matrix:
        include:
          - sys: mingw64
            env: x86_64
            native_comp: --without-native-compilation
          - sys: mingw64
            env: x86_64
            native_comp: --with-native-compilation=aot
          - sys: ucrt64
            env: ucrt-x86_64
            native_comp: --without-native-compilation
          - sys: ucrt64
            env: ucrt-x86_64
            native_comp: --with-native-compilation=aot
    steps:
      - name: git config
        run: git config --global core.autocrlf false
        shell: bash
      - name: setup msys2
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.sys }}
          update: true
          cache: false
          install: >-
            zip
            unzip
      - uses: actions/checkout@v6
      - name: build
        shell: msys2 {0}
        run: |
          chmod +x build-emacs.sh
          ./build-emacs.sh https://git.savannah.gnu.org/git/emacs.git master ${{ matrix.native_comp }} ${{ matrix.sys }} ${{ matrix.env }}
          native_comp=`echo "${{ matrix.native_comp}}" | sed -e 's/=/-/g' -e 's/--/-/g' -e 's/^-//' -e 's/-$//'`
          echo "native_comp=${native_comp}" >> $GITHUB_ENV
          cd /c/programs/
          mv emacs.zip emacs-${{ needs.prepare.outputs.emacs_ver }}-${{ matrix.env }}-${native_comp}.zip
      - name: upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: emacs-${{ needs.prepare.outputs.emacs_ver }}-${{ matrix.env }}-${{ env.native_comp }}
          path: C:/programs/emacs-*.zip
          if-no-files-found: error
  release:
    runs-on: ubuntu-latest
    needs: [prepare, build]
    steps:
      - uses: actions/checkout@v6
      - name: download artifact
        uses: actions/download-artifact@v6
        with:
          path: ./
          pattern: emacs-*
          merge-multiple: true
      - name: release
        run: |
          gh release delete nightly --cleanup-tag --yes
          sleep 5
          gh release create nightly emacs-*.zip --prerelease --title "emacs-${{ needs.prepare.outputs.emacs_ver }}" --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
